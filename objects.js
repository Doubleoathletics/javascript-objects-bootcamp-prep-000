var playlist 

var playlist = new Object({ artist names: 'Song titles'})
function updatePlaylist{
  obj[artist names] = song titles
  return obj
}

Object.assign({}, {})
function updatePlaylist( the playlist, 'artist name', song title) {
  return Object.assign({}, obj, {[Key]: value})
}